# Inverted-Search
The project implements an Inverted Index using hash algorithms to map words to their locations in documents, enabling fast full-text searches. It is designed for efficient document retrieval, similar to search engine indexing, and supports quick look-up of content across multiple files.
